[pytest]
addopts = --verbose --tb=short --alluredir=allure-results
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    unit: Unit tests
    e2e: End-to-end tests
    slow: Slow running tests